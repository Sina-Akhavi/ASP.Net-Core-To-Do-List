services:

  mysql:
    image: mysql:8.0

    container_name: todo-mysql

    restart: always

    environment:
      MYSQL_ROOT_PASSWORD: sinaakhavi1380
      MYSQL_DATABASE: TodoDb

    ports:
      - "3306:3306"

    volumes:
      - mysql_data:/var/lib/mysql


volumes:
  mysql_data:
